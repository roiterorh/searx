icon: https://apps.okteto.com/movies/icon.png
deploy:
  - okteto build -t okteto.dev/searx:${OKTETO_GIT_COMMIT} .
#   - helm upgrade --install movies chart --set tag=${OKTETO_GIT_COMMIT}
# devs:
#   - api/okteto.yml
#   - frontend/okteto.yml